import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Tomas Gonzalez | Full Stack Engineer",
  description: `Tomas Gonzalez is a Full Stack Engineer with 5+ years of experience building scalable, 
  accessible web apps using React and TypeScript. Proven ability to deliver high-traffic solutions in 
  government and education sectors. Skilled in CI/CD, automated testing, and collaborating closely with 
  designers and product managers.`,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
